name = "blitzpit-api"
main = "src/index.ts"
compatibility_date = "2024-09-23"

# STRIPE_SECRET_KEY is set via `wrangler secret put STRIPE_SECRET_KEY`
# Do NOT put secrets in this file -- GitHub will block the push
